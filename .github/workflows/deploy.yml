- name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          command: deploy --config .output/server/wrangler.json
        env:
          APIVEHICULO_KEY: ${{ secrets.APIVEHICULO_KEY }}
